name: fonika_translate_example
description: Example app for fonika_translate package.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  fonika_translate:
    path: ../
  flutter_dotenv: ^5.2.1

flutter:
  assets:
    - .env
